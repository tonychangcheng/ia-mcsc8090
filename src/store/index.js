import { createStore } from 'vuex'; // Use createStore for Vuex 4

export default createStore({
  state: {
    // change 'server' to your backend without '/' at the end.
    server: 'https://ia-api-qmw9.onrender.com',
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
});
